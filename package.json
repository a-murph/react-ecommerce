{
	"name": "ecommerce",
	"version": "1.0.0",
	"engines": {
		"node": "8.10.0"
	},
	"description": "",
	"main": "server.js",
	"scripts": {
		"start": "node server",
		"heroku-postbuild": "node scripts/seedDB && cd client && yarn && yarn run build",
		"test": "echo \"Error: no test specified\" && exit 1",
		"installDeps": "yarn && cd client && yarn"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^3.6.1",
		"nodemon": "^1.19.4"
	},
	"dependencies": {
		"axios": "^0.18.1",
		"body-parser": "^1.19.0",
		"express": "^4.17.1",
		"mongoose": "^5.11.4",
		"nuka-carousel": "^4.7.4",
		"react": "^16.14.0",
		"react-dom": "^16.14.0",
		"react-router-dom": "^4.3.1"
	}
}
